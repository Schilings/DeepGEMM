# GEMM-RS 迭代记录（归档摘要）

> 本文件仅保留历史迭代摘要，不作为当前实现说明。
> 当前唯一有效结论请查看 `docs/PROGRESS.md` 与 `docs/GEMM_RS_DESIGN.md`。

---

## 历史说明

早期阶段曾进行多轮结构与参数探索，包含不同机器与不同时间窗口下的实验结果。
这些记录对“为什么做过某些尝试”有价值，但**不应直接当作当前可复现状态**。

---

## 当前使用规则

1. 要看“现在能不能跑、跑得怎样”：看 `PROGRESS.md`。
2. 要看“当前主线怎么设计”：看 `GEMM_RS_DESIGN.md`。
3. 要追溯历史细节：再回看 archive 文档。

---

## 归档位置

- `docs/archive/`
- 以及仓库历史提交（`git log`）

---

## 维护约定

从本次起，不再在主文档体系中使用容易误导的多版本命名叙事；
主线只保留一个口径：`bf16_gemm_rs_nt` 的可复现正确性与性能进展。
